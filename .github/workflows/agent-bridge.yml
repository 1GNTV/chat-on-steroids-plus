name: Agent Bridge

on:
  push:
    branches: [main, remote-one-command]
    paths:
      - "agent-bridge/**"
      - "remote-client/**"
      - "install.sh"
      - "install.ps1"
      - ".github/workflows/agent-bridge.yml"
  pull_request:
    paths:
      - "agent-bridge/**"
      - "remote-client/**"
      - "install.sh"
      - "install.ps1"
      - ".github/workflows/agent-bridge.yml"

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        node: [20, 22]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: ${{ matrix.node }}
      - name: Test host bridge
        working-directory: agent-bridge
        run: npm test
      - name: Check host package
        working-directory: agent-bridge
        run: npm pack --dry-run
      - name: Test remote client
        working-directory: remote-client
        run: npm test
      - name: Check remote client package
        working-directory: remote-client
        run: npm pack --dry-run
