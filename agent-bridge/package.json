{
  "name": "chat-on-steroids-plus-bridge",
  "version": "0.2.0",
  "description": "Local and remote CLI bridge for ChatGPT coding workflows with a one-command free Quick Tunnel.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "agent-bridge": "./bin/agent-bridge.mjs",
    "cos-plus": "./bin/agent-bridge.mjs"
  },
  "files": [
    "bin",
    "src",
    "LICENSE",
    "README.md"
  ],
  "scripts": {
    "test": "node --test",
    "smoke": "node ./bin/agent-bridge.mjs --help"
  },
  "engines": {
    "node": ">=20"
  }
}
